<IfModule mod_rewrite.c>
    RewriteEngine On
    # Generic base so app works in subfolders and virtual hosts
    RewriteBase /
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>
